# Entity: coffee

main: kit: entity: coffee: {

  alias: field: {}

  fields: [
    {
      format: "uri"
      name: "file"
      req: true
      short: "URL of the random coffee image"
      type: "`$STRING`"
    }
  ]

  name: "coffee"
  op: {
    load: {
      name: "load"
      points: [
        {
          method: "GET"
          orig: "/random.json"
          segments: [
            {
              lit: "random.json"
            }
          ]
          transform: {
            req: "`reqdata`"
            res: "`body`"
          }
        }
      ]
    }
  }



}
